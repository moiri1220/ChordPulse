# AGENTS.md

## 1. このファイルの目的

本ファイルは、ChordPulseの開発に参加するすべてのエージェントが守る共通ルールを定める。複数エージェントが同じリポジトリで作業することを前提とし、変更の衝突を減らし、作業内容を引き継ぎやすくすることを目的とする。

仕様の基準は以下の順に扱う。

1. ユーザーからの最新の指示
2. 本ファイル（`AGENTS.md`）
3. [MVP仕様書](docs/mvp-specification.md)
4. [開発ロードマップ](docs/roadmap.md)
5. 各ディレクトリ配下の追加ルール

下位ディレクトリに別の`AGENTS.md`がある場合、そのディレクトリ以下ではより具体的なルールを優先する。ただし、ユーザーの指示に反するルールは適用しない。

## 2. プロジェクトの基本方針

- ChordPulseは、音源ファイルまたは利用条件を確認したYouTube URLからBPM・ビート・コードを推定し、マスターコード譜をMusicXMLとして生成・表示するWebアプリケーションである。
- MVPの対象は、入力、音源取得、音楽解析、MusicXML生成、OSMDによる表示、MusicXMLダウンロードである。
- 解析結果は推定値であり、正確性を過度に約束するUIや説明を追加しない。
- 譜面の手動編集、元音源との同期再生、アカウント、保存・共有機能は、明示的な依頼がない限りMVPへ追加しない。
- 解析エンジン、MusicXML生成、API、表示層は可能な限り分離し、将来のエンジン交換や非同期化を妨げない構造にする。
- 音源の適法利用を最優先し、著作権法第30条の4の情報解析に該当する範囲で、解析目的・保存期間・出力範囲を守る。法的適法性を自己申告だけで保証しない。
- 仕様変更や運用手順の追加・変更が必要になった場合は、コードだけでなく関連ドキュメントも更新する。特に運用に関する情報（ローカル起動手順、本番環境向けの考慮事項、環境変数等）が増えた場合は、必ず `docs/operations-guide.md` に追記・更新する。


## 3. エージェントの役割分担

作業開始時に、担当エージェントと対象ファイルまたは対象領域を明確にする。主な役割は以下を目安とする。

| 役割 | 主な担当 |
| --- | --- |
| オーケストレーター | タスク分解、依存関係の整理、統合、最終確認 |
| 解析エージェント | 音源取得、BPM・ビート・Onset、コード解析 |
| MusicXMLエージェント | 内部解析データ、music21、リズムスラッシュ生成 |
| APIエージェント | FastAPI、入力検証、エラー形式、レスポンス契約 |
| フロントエンドエージェント | 入力UI、状態表示、OSMD描画、ダウンロード |
| QAエージェント | テスト、再現確認、受け入れ条件、回帰確認 |
| ドキュメントエージェント | 仕様書、ロードマップ、README、引き継ぎ情報 |

役割は固定の所有者ではなく、タスク単位の担当である。1つのファイルを複数エージェントが同時に編集しない。

## 4. 作業開始時のルール

作業開始前に、次の確認を行う。

- ユーザーの目的と完了条件を1〜3文で整理する。
- 変更予定のファイルを確認し、他エージェントの担当と重複していないか確認する。
- 既存の未コミット変更を確認し、自分の変更と混同しない。
- 仕様書・関連実装・テストを読んでから変更する。
- 不明点があっても、安全な仮定で進められる場合は進める。API契約やデータ形式など後続作業へ大きく影響する点は、先に共有する。

作業開始の共有には、少なくとも以下を含める。

```text
担当: <役割またはエージェント名>
目的: <今回の変更の目的>
対象: <変更予定ファイルまたは領域>
依存: <先行作業・確認が必要な事項。なければ「なし」>
```

## 5. 並行作業と競合回避

- 変更前に対象ファイルの最新状態を確認する。
- 同じファイルを触る必要がある場合は、担当エージェント間で範囲を分割するか、先に作業を完了したエージェントから引き継ぐ。
- 他エージェントの変更を上書きしない。意図が分からない変更は削除・巻き戻しせず、確認してから対応する。
- 大きな機械的変更や全体フォーマットは、並行作業中に実行しない。
- 依存する変更が未完了の場合は、仮のインターフェースを勝手に固定せず、契約案を共有してから実装する。
- 自分の担当外の改善を見つけても、同じ変更に混ぜない。課題として記録し、必要なら別タスクに分ける。
- 別ブランチや別ワークツリーを使う場合も、統合前に対象ファイルの競合と仕様整合性を確認する。

## 6. 実装ルール

### 6.1 共通

- 変更は目的に必要な最小範囲に留める。
- 既存の命名・ディレクトリ構成・フォーマットを尊重する。
- 一時ファイル、音源、生成物、認証情報をGitへ追加しない。
- 秘密情報はコードやログへ書き込まない。設定値は環境変数またはローカル設定で扱う。
- 外部コマンドやユーザー入力は、形式・サイズ・実行時間を検証する。
- エラーメッセージは、利用者向けには理解しやすく、開発者向けログには原因を追える情報を残す。
- 解析処理はAPI層から分離し、CLI・テスト・将来の非同期ジョブから再利用できるようにする。

### 6.2 音源解析

- 音源の入力形式を正規化してから解析する。
- BPM、拍位置、コード区間などの中間結果は、単位と時間基準を明確にする。
- 時間は原則として秒、拍位置は明示された拍インデックスで扱う。暗黙の丸めを行わない。
- コード解析エンジンを差し替えられるよう、エンジン固有の出力を内部データ形式へ変換する層を置く。
- 未判定区間、無音、途中終了、テンポ変動を想定する。
- 音源を扱うテストでは、著作権上問題のない短いテスト素材または合成音源を使う。

### 6.3 MusicXML

- MusicXML生成は、解析結果と表記レベルを入力とする独立した処理として扱う。
- レベル1〜3の違いがコード上でも追跡できるようにする。
- 小節、拍子、音価、コードシンボルの整合性を保つ。
- 生成したMusicXMLはOSMDまたはMusicXML対応ビューアで開けることを確認する。
- 解析結果を変更せず、表示・記譜上の変換はMusicXML生成層に閉じ込める。

### 6.4 API

- 入力バリデーションをAPI境界で行う。
- `file`と`youtube_url`は一方のみ受け付け、`lawful_use_confirmation=true`を必須とする。YouTube URL機能は設定で無効化できる。
- `rhythm_level`は`1`、`2`、`3`のみ受け付ける。
- エラーはフロントエンドが扱える構造化された形式で返す。
- MusicXML本体とメタデータ（推定BPMなど）の返却方法を変更する場合は、フロントエンド担当と先に契約を更新する。
- 長時間処理を想定し、処理ロジックを同期HTTPリクエストへ密結合しない。

### 6.5 フロントエンド

- 解析開始、解析中、成功、失敗の状態を明示する。
- 解析中は二重送信を防ぐ。
- 解析結果が推定値であることを必要な範囲で説明する。
- OSMDへ渡すMusicXMLとダウンロードするMusicXMLが一致することを確認する。
- APIエラーをそのまま表示せず、利用者が次に取る行動を判断できるメッセージへ変換する。

### 6.6 音源の適法利用と権利保護

- 音源解析は、著作権法第30条の4の情報解析に該当する範囲で、音源の鑑賞・再配信ではなくコード、BPM、拍位置、リズム等の抽出を目的とする。
- 法第30条の4の適用を過度に一般化しない。必要な限度を超える利用、著作権者の利益を不当に害する利用、契約・規約に反する利用は実装しない。
- 利用者は適法に入手・利用できる音源だけを扱う。違法アップロードと知りながらの取得、アクセス制限・DRM・コピー防止の回避を禁止する。
- `lawful_use_confirmation=true`を解析前に必須とする。権利侵害の申告や明らかに問題のある入力は処理を停止する。
- 音源と中間音声は解析中だけ隔離された一時領域に置き、解析完了・失敗・キャンセル時に削除する。起動時・定期処理で削除漏れを掃除する。
- 音源の永続保存、公開URL、ユーザー間共有、音源の再ダウンロードを実装しない。
- MusicXMLには音源、波形、歌詞、メロディー、長い音声断片を含めず、コードネーム、BPM、拍子、リズムスラッシュ等のマスターコード譜に限定する。
- `yt-dlp`等によるYouTube取得は、現行の利用規約・APIポリシー・取得方法が許す場合だけ使用する。規約が不明・不許可の場合は設定で機能を無効化する。
- YouTubeの公式プレイヤーで視聴できることを、音声のダウンロード許可とみなさない。
- 権利侵害の申告・削除依頼を受けた場合は対象処理を停止し、音源・中間データ・生成物を速やかに削除する。
- 判断に迷う場合は処理を停止し、運用責任者または法律専門家へ確認する。利用者のチェックだけで適法性を断定しない。

## 7. テストと検証

変更内容に応じて、必要な検証を実施する。テストを実行できない場合は、理由と代替確認方法を報告する。

最低限、該当する範囲で以下を確認する。

- 入力バリデーション：正常なMP3/WAV、正常なURL、不正形式、同時指定、未指定
- 権利保護：確認同意の不足、違法入手と知りながらの入力、アクセス制限回避、設定で無効化されたYouTube URLが音源取得前に拒否されること
- 解析：BPM・拍位置・コード区間の基本ケース、未判定区間、無音や短い音源
- MusicXML：レベル1〜3、複数小節、コード切り替え、MusicXMLパース可否
- API：成功レスポンス、各エラーコード、エラー本文の形式
- UI：解析中の状態、失敗時の表示、譜面表示、ダウンロード
- 回帰：変更していないPhaseの既存機能

検証結果は、実行したコマンドと結果を簡潔に残す。

```text
検証:
- `命令`: 成功 / 失敗 / 未実行
- `命令`: 成功 / 失敗 / 未実行
補足: <失敗理由、未実行理由、既知の問題>
```

## 8. 変更の完了条件

担当エージェントは、作業完了時に以下を満たす。

- 依頼された範囲だけが変更されている。
- 関連テストまたは代替検証を実施している。
- 新しい設定、依存関係、環境変数、実行手順があれば文書化している。
- 仕様変更や運用手順の追加・更新があれば`docs/`の関連文書（特に`docs/operations-guide.md`）を更新している。
- 未解決の問題、既知の制約、後続作業を明記している。

- オーケストレーターへ引き継ぎ可能なサマリーを共有している。

完了時の共有形式は以下を使用する。

```text
完了: <変更の要約>
変更ファイル: <ファイル一覧>
検証: <実行した検証と結果>
未解決: <なければ「なし」>
後続: <依存する作業や注意点。なければ「なし」>
```

## 9. コミット・統合ルール

- 1コミット1目的を基本とする。
- コミットメッセージは変更内容が分かる命令形または簡潔な説明にする。
- 仕様、実装、テストを無関係な変更と混ぜない。
- 統合前に、変更ファイル、テスト結果、依存関係、API契約の差分を確認する。
- コンフリクト解消後は、解消した箇所に関係するテストを再実行する。
- エージェント間の作業が未完了のまま統合しない。未完了の場合は、仮実装であることを明記する。

## 10. 判断に迷った場合

1. 最新のユーザー指示とMVP受け入れ条件を確認する。
2. 変更範囲を最小化し、後から差し替え可能な境界を選ぶ。
3. 他エージェントの担当範囲に影響する場合は、契約案と影響範囲を共有する。
4. 破壊的変更、外部サービスへの実行、著作権・認証・個人情報に関わる判断は独断で進めない。
5. 判断を保留した場合は、保留理由と推奨案を引き継ぎ情報へ残す。

## 11. 法令・公式規約の確認先

法令・規約は変更されるため、リリース前、音源取得方法の変更時、外部サービスの変更時に最新版を確認する。以下は設計上の参照先であり、個別案件の法的判断を代替しない。

- [著作権法（e-Gov法令検索）](https://laws.e-gov.go.jp/law/345AC0000000048)
- [文化庁「著作権法の一部を改正する法律（平成30年法律第30号）について」](https://www.bunka.go.jp/seisaku/chosakuken/hokaisei/h30_hokaisei/)
- [文化庁「ここが知りたい著作権」](https://www.bunka.go.jp/seisaku/chosakuken/taisetsu/point/index.html)
- [YouTube Terms of Service](https://www.youtube.com/t/terms?gl=JP)
- [YouTube API サービス デベロッパーポリシー](https://developers.google.com/youtube/terms/developer-policies?hl=ja)
